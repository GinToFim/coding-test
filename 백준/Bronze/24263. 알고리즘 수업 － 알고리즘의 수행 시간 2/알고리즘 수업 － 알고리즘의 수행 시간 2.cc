#include <iostream>

using namespace std;

int n;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> n;
    cout << n << '\n';
    cout << 1 << '\n';
    return 0;
}